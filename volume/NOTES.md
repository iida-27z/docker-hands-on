# volume ディレクトリについて

このディレクトリはDockerのボリュームマウントの学習用に作成したものです。

READMEのハンズオンでは、このディレクトリをホスト側の作業場所として使います。

- ホスト側では、この配下に`hello.py`などのファイルを作成します。
- コンテナ側では、`/workspace`として同じ内容を参照します。
- ホスト側とコンテナ側の変更は、ボリュームマウントを通じて相互に反映されます。
